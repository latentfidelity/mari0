# Conversion Ledger

This file tracks deliberate Lua-to-Rust moves. Update it when a system changes ownership.

| Source | Rust Target | Status | Verification Gate |
| --- | --- | --- | --- |
| `graphics/SMB/title.png`, `graphics/icon.png` | n/a | Rebranded baseline asset | Manual visual check |
| `main.lua` app identity/update settings | n/a | Lua baseline adjusted | `git diff --check` |
| n/a | `iw2wth_core::math` | Seeded | `cargo test --manifest-path rust/Cargo.toml` |
| n/a | `iw2wth_core::level` | Seeded | `cargo test --manifest-path rust/Cargo.toml` |
| `physics.lua` `portalcoords` representative behavior | `iw2wth_core::wormhole::legacy_portal_coords` | Seeded | Rust tests for facing changes, animation flip, and live up-exit min speed |
| `physics.lua` `portalcoords` facing-pair fixtures | `iw2wth_core` legacy portalcoords integration fixture test | Seeded | Rust integration test consumes `legacy_portalcoords.generated.tsv` covering all 16 facing pairs |
| `mario.lua` on-vine player frame-step branch | `iw2wth_core` legacy player frame-step integration fixture test | Seeded | Rust integration test consumes `legacy_player_frame_steps.generated.tsv` covering up/down/idle and blocked down-vine frame steps |
| all Lua modules | `xtask inventory` | Inventory generated | Regenerate `docs/lua-inventory.generated.md` after major Lua refactors |
| `variables.lua` core movement/gravity constants | `iw2wth_core::config` | Seeded | `cargo test --manifest-path rust/Cargo.toml` |
| `physics.lua` `aabb` and collision-axis decision | `iw2wth_core::collision` | Seeded | `cargo test --manifest-path rust/Cargo.toml` |
| `game.lua` `inrange` | `iw2wth_core::collision::in_range` | Seeded | `cargo test --manifest-path rust/Cargo.toml` |
| `mappacks/*/settings.txt` metadata | `iw2wth_core::content::MappackSettings` | Seeded | Includes `smb` and `dlc_the_untitled_game` fixtures |
| `mappacks/*/*.txt` level grid/properties | `iw2wth_core::content::Mari0Level` | Seeded | Parses `mappacks/smb/1-1.txt` fixture |
| bundled mappack corpus | `xtask validate-content` | Seeded | Parses 8 mappacks and 248 level files with 0 errors |
| legacy entity usage | `xtask validate-content` entity usage report | Seeded | Reports 93 used entity types, 6068 entity cells, and 1317 link targets |
| `entity.lua` high-use entity IDs and mappack cell payloads | `iw2wth_core::content` legacy entity placement types | Seeded | Rust tests cover high-use ID mapping, raw right-click parameters, link preservation, and unknown fallback |
| `entity.lua` power-up entity IDs 2-5 and mappack cell payloads | `iw2wth_core::content::LegacyPowerUpEntity` | Seeded | Rust tests cover mushroom, one-up, star, and many-coins ID mapping with empty parameter/link payloads |
| `entity.lua` singleton marker IDs 8, 25, 35, 40, 70 and mappack cell payloads | `iw2wth_core::content::LegacyEntityKind` singleton variants | Seeded | Rust tests cover player spawn, maze gate number parameters, drain, button links, and plant empty payloads |
| `entity.lua` ground light entity IDs 43-48 and mappack cell payloads | `iw2wth_core::content::LegacyGroundLightEntity` | Seeded | Rust tests cover all orientation mappings and link preservation |
| `entity.lua` gel tile entity IDs 85-88 and mappack cell payloads | `iw2wth_core::content::LegacyEntitySurface` | Seeded | Rust tests cover all surface mappings and gel ID parameter preservation |
| `entity.lua` linked/logic entity IDs 26-30, 74, 84 and mappack cell payloads | `iw2wth_core::content::LegacyLinkedEntity` | Seeded | Rust tests cover door/emancipation/wall indicator/timer/not-gate ID mapping, raw timer parameters, link preservation, and unknown fallback |
| `entity.lua` platform entity IDs 18, 19, 32, 41, 42, 80, 92 and mappack cell payloads | `iw2wth_core::content::LegacyPlatformEntity` | Seeded | Rust tests cover platform ID mapping, raw fractional width parameters, integer spawner/seesaw parameters, empty bonus platform payloads, and unknown fallback |
| `entity.lua` warp entity IDs 14, 21, 31, 81 and mappack cell payloads | `iw2wth_core::content::LegacyWarpEntity` | Seeded | Rust tests cover pipe/vine/pipe-spawn/warp-pipe ID mapping, raw destination parameters, and unknown fallback |
| `entity.lua` lightbridge entity IDs 36-39 and mappack cell payloads | `iw2wth_core::content::LegacyLightBridgeEntity` | Seeded | Rust tests cover directional lightbridge ID mapping and empty parameter/link payloads |
| `entity.lua` laser entity IDs 52-59 and mappack cell payloads | `iw2wth_core::content::LegacyLaserEntity` | Seeded | Rust tests cover laser emitter/detector ID mapping, empty emitter payloads, detector link preservation, and unknown fallback |
| `entity.lua` gel dispenser entity IDs 61-66, 71-73 and mappack cell payloads | `iw2wth_core::content::LegacyGelDispenserEntity` | Seeded | Rust tests cover blue/orange/white dispenser ID mapping and empty parameter/link payloads |
| `entity.lua` faithplate entity IDs 49-51 and mappack cell payloads | `iw2wth_core::content::LegacyFaithPlateEntity` | Seeded | Rust tests cover up/right/left faithplate ID mapping and empty parameter/link payloads |
| `entity.lua` box/pushbutton entity IDs 20, 67-69 and mappack cell payloads | `iw2wth_core::content::LegacyBoxEntity` | Seeded | Rust tests cover companion cube, box tube, and pushbutton ID mapping, empty cube payloads, and tube/button link preservation |
| `entity.lua` fire hazard entity IDs 79, 82, 89, 97 and mappack cell payloads | `iw2wth_core::content::LegacyHazardEntity` | Seeded | Rust tests cover castle fire/fire-start/upfire ID mapping, castle fire length parameters, and empty fire-start/upfire payloads |
| `entity.lua` goal/progression entity IDs 11, 91, 100 and mappack cell payloads | `iw2wth_core::content::LegacyGoalEntity` | Seeded | Rust tests cover flag/axe/checkpoint ID mapping and empty parameter/link payloads |
| `entity.lua` boss entity ID 90 and mappack cell payloads | `iw2wth_core::content::LegacyEnemyEntity::Bowser` | Seeded | Rust tests cover Bowser ID mapping and empty parameter/link payloads |
| `entity.lua` bullet-bill launcher entity ID 60 and mappack cell payloads | `iw2wth_core::content::LegacyEnemyEntity::BulletBillLauncher` | Seeded | Rust tests cover bullet-bill launcher ID mapping and empty parameter/link payloads |
| `entity.lua` Goomba-family enemy entity IDs 9, 98, 99 and mappack cell payloads | `iw2wth_core::content::LegacyEnemyEntity` Goomba-family placement variants | Seeded | Rust tests cover goombahalf, spikey, and spikeyhalf ID mapping with empty parameter/link payloads |
| `entity.lua` Koopa-family enemy entity IDs 10, 12, 13, 75, 76, 77, 78 and mappack cell payloads | `iw2wth_core::content::LegacyEnemyEntity` Koopa-family placement variants | Seeded | Rust tests cover koopahalf, koopared, kooparedhalf, beetle, beetlehalf, kooparedflying, and koopaflying ID mapping with empty parameter/link payloads |
| `entity.lua` Hammer Bro enemy entity ID 15 and mappack cell payloads | `iw2wth_core::content::LegacyEnemyEntity::HammerBro` | Seeded | Rust tests cover hammerbro ID mapping with empty parameter/link payloads |
| `entity.lua` Cheep-family enemy entity IDs 16, 17 and mappack cell payloads | `iw2wth_core::content::LegacyEnemyEntity` Cheep-family placement variants | Seeded | Rust tests cover cheepred and cheepwhite ID mapping with empty parameter/link payloads |
| `entity.lua` Lakito enemy entity ID 22 and mappack cell payloads | `iw2wth_core::content::LegacyEnemyEntity::Lakito` | Seeded | Rust tests cover lakito ID mapping with empty parameter/link payloads |
| `entity.lua` squid enemy entity ID 94 and mappack cell payloads | `iw2wth_core::content::LegacyEnemyEntity::Squid` | Seeded | Rust tests cover squid ID mapping with empty parameter/link payloads |
| `entity.lua` spring entity ID 93 and mappack cell payloads | `iw2wth_core::content::LegacyEntityKind::Spring` | Seeded | Rust tests cover spring ID mapping and empty parameter/link payloads |
| `entity.lua` level-control marker entity IDs 23, 24, 33, 34, 83, 95, 96 and mappack cell payloads | `iw2wth_core::content::LegacyLevelControlEntity` | Seeded | Rust tests cover maze start/end, bullet-bill start/end, Lakito end, and flying-fish start/end ID mapping with empty parameter/link payloads |
| `mario.lua` normal horizontal movement and jump impulse | `iw2wth_core::player` | Seeded | Rust tests cover walk/run acceleration, friction, airborne thresholds, ducking friction, jump impulse, and legacy turnaround quirks |
| `variables.lua` underwater movement constants | `iw2wth_core::config::UnderwaterMovementConstants` | Seeded | Rust constants test matches `uw*` movement/gravity values |
| `mario.lua` underwater horizontal movement and swim jump impulse | `iw2wth_core::player` | Seeded | Rust tests cover underwater walk/air caps, run-key ignore, no-input friction, ducking-with-direction behavior, surface pushdown, and swim jump impulse |
| `variables.lua` orange-gel movement constants | `iw2wth_core::config::OrangeGelMovementConstants` | Seeded | Rust constants test matches `gel*` movement values |
| `mario.lua` orange-gel movement overrides | `iw2wth_core::player` surface query wrapper | Seeded | Rust tests cover Mari0 ground probe math, in-map guard, orange-only overrides, boosted acceleration, and airborne non-query behavior |
| `variables.lua` player animation speed constants | `iw2wth_core::config::PlayerAnimationConstants` | Seeded | Rust constants test matches `runanimationspeed` and `swimanimationspeed` |
| `mario.lua` run/swim frame advancement | `iw2wth_core::player` animation advance functions | Seeded | Rust tests cover initial frame state, run wrapping, non-running no-op, underwater run/swim branches, and legacy swim use of run animation speed |
| `mario.lua` player gravity selection | `iw2wth_core::player` vertical state helpers | Seeded | Rust tests cover normal/underwater gravity, jump-to-fall flag transition, and preserved jump animation state |
| `physics.lua` gravity velocity application | `iw2wth_core::player` vertical state helpers | Seeded | Rust tests cover gravity acceleration, max fall-speed clamp, and exact startfall check after vertical movement |
| `mario.lua` basic floor/head collision state | `iw2wth_core::player` collision state helpers | Seeded | Rust tests cover landing idle/running/sliding state, clearing jump/fall flags, and head-bump speed/state response |
| `variables.lua` horizontal blue-gel bounce constants | `iw2wth_core::config::BlueGelBounceConstants` | Seeded | Rust constants test matches `horbounce*` values and legacy floor threshold multiplier |
| `mario.lua` blue-gel floor/side player bounce | `iw2wth_core::player` blue-gel bounce helpers | Seeded | Rust tests cover floor threshold/down-input gate, gravity reapplication, side airborne/speed/down-input gates, side velocity transforms, and right-side legacy asymmetry |
| `mario.lua` enemy stomp player bounce speed/state | `iw2wth_core::player` enemy stomp bounce helpers | Seeded | Rust tests cover `sqrt(2*gravity*bounceheight)` speed calculation, jump animation state, falling flag, and preserved jumping flag |
| `variables.lua` Goomba constants and `goomba.lua` basic Goomba state machine | `iw2wth_core::config::LegacyGoombaConstants` and `iw2wth_core::enemy` | Seeded | Rust tests cover spawn offsets, walking animation timer threshold, speed easing, side turnarounds/default suppression, stomp expiry, shot impulse/gravity step, and portal rotation recovery |
| `goomba.lua` walking spikey variant | `iw2wth_core::enemy::LegacyGoombaVariant::Spikey` | Seeded | Rust tests cover shared Goomba spawn/body/motion state, spikey frame initialization, strict animation threshold frame toggles, and side-collision animation direction |
| `goomba.lua` falling/Lakito-thrown spikey variant and Lakito collision hook | `iw2wth_core::enemy::LegacyGoombaVariant::SpikeyFalling` and falling spikey helpers | Seeded | Rust tests cover thrown spawn speed/gravity/frame state, rotation reset, no walking speed easing, Lakito mask decay threshold, Lakito collision suppression, floor conversion to walking spikey, direction choice from player positions, and the legacy nearest-player comparison quirk |
| `variables.lua` Koopa constants and `koopa.lua` walking/shell/flying state | `iw2wth_core::config::LegacyKoopaConstants` and `iw2wth_core::enemy` | Seeded | Rust tests cover spawn offsets, flying gravity/frame setup, animation threshold, walking and shell speed easing, shell stomp start/stop positioning, flying stomp gravity restore, side-collision default suppression, floor bounce, start-fall flag, and shot impulse/gravity step |
| `koopa.lua` red Koopa edge-turn map query | `iw2wth_core::enemy::apply_legacy_red_koopa_edge_turn` | Seeded | Rust tests cover gap/solid-neighbor gates, left/right snap formulas, speed flip, animation direction, and ineligible green/flying/shell/falling states |
| `koopa.lua` beetle variant and `fireball.lua` beetle fireball resistance | `iw2wth_core::enemy::LegacyKoopaVariant::Beetle` and `legacy_koopa_resists_fireball` | Seeded | Rust tests cover beetle walking/shell spawn state, non-red edge-turn ineligibility, and beetle-only fireball resistance |
| `variables.lua` plant constants and `plant.lua` pipe plant cycle | `iw2wth_core::config::LegacyPlantConstants` and `iw2wth_core::enemy` | Seeded | Rust tests cover spawn offsets, animation timer threshold, movement out/in clamps, player-near hold/reset callback bounds, and shot removal |
| `variables.lua` bullet bill constants and `bulletbill.lua` projectile state | `iw2wth_core::config::LegacyBulletBillConstants` and `iw2wth_core::enemy` | Seeded | Rust tests cover projectile spawn offsets, lifetime removal threshold, scissor-release threshold, rotation snap, animation direction priority, stomp/shot direct motion, offscreen removal, and portal kill flag |
| `bulletbill.lua` rocket launcher timer and fire decision | `iw2wth_core::enemy::update_legacy_bullet_bill_launcher` and `fire_legacy_bullet_bill_launcher` | Seeded | Rust tests cover initial timer offset, strict timer gate, strict viewport gate, max-count rejection, nearest-player probe math, first-player tie behavior, range-based left/right decisions, timer reset, and injected next-fire time |
| `variables.lua` Cheep Cheep constants and `cheepcheep.lua` swimming/shot state | `iw2wth_core::config::LegacyCheepCheepConstants` and `iw2wth_core::enemy::LegacyCheepCheepState` | Seeded | Rust tests cover red/white speed selection, injected vertical movement random choice, strict animation timing with the uninitialized-frame quirk, vertical range turns, shot impulse/gravity motion, rotation recovery, and collision suppression |
| `variables.lua` squid constants and `squid.lua` swim/shot state | `iw2wth_core::config::LegacySquidConstants` and `iw2wth_core::enemy::LegacySquidState` | Seeded | Rust tests cover spawn offsets, idle trigger math, strict nearest-player tie preservation, upward/downward swim transitions, direction choice, shot impulse/gravity motion, rotation recovery, and collision suppression |
| `variables.lua` Lakito constants and `lakito.lua` active/shot/passive state | `iw2wth_core::config::LegacyLakitoConstants` and `iw2wth_core::enemy::LegacyLakitoState` | Seeded | Rust tests cover spawn offsets, strict throw and respawn timers, max-spikey gate, projected-player tracking with preserved nearest-player quirk, passive Lakito-end update order, shot/stomp state, and spikeyfall collision suppression |
| `variables.lua` hammer constants and `hammerbro.lua` thrown hammer projectile state | `iw2wth_core::config::LegacyHammerConstants` and `iw2wth_core::enemy::LegacyHammerState` | Seeded | Rust tests cover spawn offsets, left/right speed selection, gravity/start speed constants, strict four-frame animation timing, mask-slot 20 decay, portal kill flag, and collision suppression |
| `variables.lua` Hammer Bro constants and `hammerbro.lua` active/shot/collision state | `iw2wth_core::config::LegacyHammerBroConstants` and `iw2wth_core::enemy::LegacyHammerBroState` | Seeded | Rust tests cover spawn offsets, active patrol bounds, strict throw and jump timers, injected random jump choice, nearest-player turning, animation/prepare frames, speed easing including the negative overshoot quirk, shot motion/removal, stomp, side collision speed/default suppression, ceiling/floor kill interactions, portal jump-mask clearing, emancipation kill, and the preserved nil-direction shot quirk |
| `variables.lua` flying fish constants and `flyingfish.lua` flying/shot state | `iw2wth_core::config::LegacyFlyingFishConstants` and `iw2wth_core::enemy::LegacyFlyingFishState` | Seeded | Rust tests cover spawn height/speed injection, zero-speed correction, animation direction, strict animation timing with the uninitialized-frame quirk, shot impulse/gravity motion, non-modulo rotation recovery, stomp default shot direction, and collision suppression |
| `variables.lua` fireball constants and `fireball.lua` projectile state/collisions | `iw2wth_core::config::LegacyFireballConstants` and `iw2wth_core::projectile::LegacyFireballState` | Seeded | Rust tests cover spawn offsets, left/right speed, strict star-animation timing, explosion release/removal, offscreen release gates including inactive-X and active-Y behavior, side/floor/ceiling/passive collision default contracts, block-hit sound events, enemy shot direction, score outputs, and Bowser/beetle fireball quirks |
| `variables.lua` fire constants and `fire.lua` hazard state/collisions | `iw2wth_core::config::LegacyFireConstants` and `iw2wth_core::hazard::LegacyFireState` | Seeded | Rust tests cover static and Bowser spawn offsets, injected Bowser target random drop, source tagging, strict two-frame animation timing, leftward motion, vertical target seeking with clamp, and collision suppression |
| `variables.lua` upfire constants and `upfire.lua` hazard state/collisions | `iw2wth_core::config::LegacyUpFireConstants` and `iw2wth_core::hazard::LegacyUpFireState` | Seeded | Rust tests cover hidden spawn position, manual gravity and motion, no-removal update contract, strict delay relaunch timing with injected next delay, preserved new-delay timer subtraction, zero-delay launch guard, and collision suppression |
| `variables.lua` castle fire constants and `castlefire.lua` rotating hazard state | `iw2wth_core::config::LegacyCastleFireConstants` and `iw2wth_core::hazard::LegacyCastleFireState` | Seeded | Rust tests cover default and explicit length/direction spawn, initial child segment placement, strict angle timer, clockwise modulo and counter-clockwise wrap behavior, strict four-frame animation timing, and derived child segment position/frame state |
| `variables.lua` firework constants and `firework.lua` boom effect state | `iw2wth_core::config::LegacyFireworkConstants` and `iw2wth_core::effects::LegacyFireworkBoomState` | Seeded | Rust tests cover injected random spawn offsets, immediate score delta, sound threshold crossing, strict duration removal, and strict thirds-based frame selection |
| `variables.lua` `coinblockdelay` and `coinblockanimation.lua` effect state | `iw2wth_core::config::LegacyCoinBlockAnimationConstants` and `iw2wth_core::effects::LegacyCoinBlockAnimationState` | Seeded | Rust tests cover spawn position, strict animation delay, multiple-frame advancement, inclusive frame-31 removal, and the legacy floating-score event without mutating the score total |
| `variables.lua` block debris constants and `blockdebris.lua` effect state | `iw2wth_core::config::LegacyBlockDebrisConstants` and `iw2wth_core::effects::LegacyBlockDebrisState` | Seeded | Rust tests cover spawn position/speed, strict animation toggle timing, multiple toggles, gravity-before-motion update order, and strict `y > 15` removal |
| `variables.lua` scrolling-score constants, `scrollingscore.lua` state, and `game.lua` score draw offsets | `iw2wth_core::config::LegacyScrollingScoreConstants` and `iw2wth_core::effects::LegacyScrollingScoreState` | Seeded | Rust tests cover scroll-relative X capture at spawn, strict lifetime removal, numeric score label preservation, 1up label preservation, and presentation offsets for numeric scores versus 1up |
| `variables.lua` coin-block reward constants and `mario.lua` coin-block reward branches | `iw2wth_core::config::LegacyCoinBlockRewardConstants` and `iw2wth_core::block::legacy_coin_block_reward` | Seeded | Rust tests cover used-block tile mapping for spriteset/invisible combinations, normal coin-block output, exact 100-coin life reward/reset behavior, disabled-life one-up sound behavior, many-coins timer start/active/expired behavior, animation spawn, coin sound, and score output |
| `mario.lua` breakable block destruction branch | `iw2wth_core::block::legacy_breakable_block_hit` | Seeded | Rust tests cover portal endpoint/adjacent-tile protection, unrelated portal non-blocking behavior, clear tile ID 1 output, tile object removal, gel clearing, block-break sound, 50-point score output, four legacy debris spawns, and sprite-batch regeneration |
| `game.lua` `jumpitems` and `mario.lua` `hitblock` item-on-top jump loop | `iw2wth_core::block::legacy_block_item_jump_requests` | Seeded | Rust tests cover mushroom/one-up jump request selection, inclusive horizontal center range checks, exact tile-top bottom alignment, missing jump-handler suppression, and source block X propagation |
| `game.lua` `enemies` and `mario.lua` `hitblock` enemy-on-top shot loop | `iw2wth_core::block::legacy_block_enemy_shot_requests` | Seeded | Rust tests cover top-of-block enemy shot request selection, inclusive horizontal center range checks, exact tile-top bottom alignment, left/right shot direction choice from block center, 100-point score event placement, missing shotted-handler suppression, and duplicate probe preservation |
| `mario.lua` `hitblock` top-coin collection branch plus `collectcoin` side effects | `iw2wth_core::block::legacy_block_top_coin_collection` | Seeded | Rust tests cover the optional top-coin guard, tile clear to ID 1, coin sound, coin-block animation spawn at the top-coin position, 200-point score output, and exact 100-coin life reward/reset behavior |
| `mario.lua` `hitblock` block-bounce queue scheduling branch | `iw2wth_core::block::legacy_block_bounce_schedule` | Seeded | Rust tests cover epsilon timer start, block coordinate capture, empty-hit scheduling, mushroom/one-up/star/vine replay payload mapping, manycoins suppression to empty content, hitter-size propagation, and bounce-start sprite-batch regeneration output |
| `mario.lua` `hitblock` contained reward-block reveal branch | `iw2wth_core::block::legacy_block_contained_reward_reveal` | Seeded | Rust tests cover empty/manycoins no-op behavior, used-block tile mapping for spriteset/invisible combinations, and vine-versus-mushroom appearance sound selection |
| `mario.lua` `hitblock` empty breakable-block destroy gate | `iw2wth_core::block::legacy_empty_breakable_block_destroy` | Seeded | Rust tests cover missing/small hitter rejection, coin-block and non-empty-content suppression, delegation into breakable-block destruction effects, and portal-protected no-op passthrough |
| `mario.lua` `hitblock` block-hit sound gate | `iw2wth_core::block::legacy_block_hit_sound_requested` | Seeded | Rust tests cover in-map non-editor hit sound emission plus suppression by the portal guard, editor mode, and out-of-map early returns |
| `game.lua` blockbounce completion replay/removal branch | `iw2wth_core::block::update_legacy_block_bounce_completion` | Seeded | Rust tests cover timer clamp on strict completion crossing, empty-entry removal without replay, mushroom-to-flower upgrade for big hitters, vine replay coordinates, and the exact-threshold no-delete quirk |
| `game.lua` blockbounce batch removal and sprite-batch regeneration branch | `iw2wth_core::block::prune_legacy_completed_block_bounces` | Seeded | Rust tests cover descending index removal over completed entries, no-op behavior with an empty delete set, and sprite-batch regeneration when any bounce entry is pruned |
| `game.lua` `coinblocktimers` positive-only countdown branch | `iw2wth_core::block::update_legacy_many_coins_timer` | Seeded | Rust tests cover positive countdown, preserved negative overshoot after crossing zero, and no-op behavior once the timer is non-positive |
| `mario.lua` `hitblock` `coinblocktimers` lookup branch | `iw2wth_core::block::legacy_many_coins_timer_state` | Seeded | Rust tests cover missing-timer detection, matching block-coordinate lookup, and preserved last-match semantics when duplicate timer entries target the same block |
| `variables.lua` item constants and `mushroom.lua` item state/collisions | `iw2wth_core::config::LegacyPowerUpConstants` and `iw2wth_core::item::LegacyMushroomState` | Seeded | Rust tests cover spawn offsets/body flags, strict emergence threshold behavior, portal rotation recovery, destroy removal, side bounce/default suppression, player growth collection outputs, floor/ceiling default contracts, and block-jump impulse/direction |
| `variables.lua` item constants and `oneup.lua` item state/collisions | `iw2wth_core::config::LegacyPowerUpConstants` and `iw2wth_core::item::LegacyOneUpState` | Seeded | Rust tests cover spawn offsets/body flags with the legacy inactive start, strict emergence threshold behavior, portal rotation recovery, post-emergence offscreen/destroy removal, side bounce/default suppression, life reward/score/sound collection outputs, floor/ceiling default contracts, and block-jump impulse/direction |
| `variables.lua` item constants and `star.lua` item state/collisions | `iw2wth_core::config::LegacyPowerUpConstants` and `iw2wth_core::item::LegacyStarState` | Seeded | Rust tests cover spawn offsets/body flags, gravity, strict emergence threshold and half-jump launch, strict animation delay with four-frame wrap, portal rotation recovery, destroy removal, side bounce/default suppression, active-only floor/ceiling star-power collection outputs, floor bounce/default suppression, and block-jump impulse/direction |
| `variables.lua` item constants and `flower.lua` item state/collisions | `iw2wth_core::config::LegacyPowerUpConstants` and `iw2wth_core::item::LegacyFlowerState` | Seeded | Rust tests cover spawn offsets/body flags, gravity-free state, strict greater-than emergence snap, strict animation delay with four-frame wrap, no portal rotation recovery, destroy removal, side default suppression, active-only floor/ceiling player growth outputs, and no-op block-jump behavior |
| `variables.lua` spring constants | `iw2wth_core::config::SpringConstants` | Seeded | Rust constants test matches `springhighforce`, `springforce`, and legacy leave-spring Y offset |
| `spring.lua` spring object state | `iw2wth_core::spring::{LegacySpringState, apply_legacy_spring_hit, update_legacy_spring}` | Seeded | Rust tests cover spawn/body flags, hit timer reset, fully-extended no-op behavior, strict timer clamp, and the legacy compression frame sequence |
| `variables.lua` vine constants and `vine.lua` object state | `iw2wth_core::config::LegacyVineConstants` and `iw2wth_core::vine::{LegacyVineState, update_legacy_vine}` | Seeded | Rust tests cover spawn/body flags, start-variant limit selection, upward growth timing, limit clamp behavior, and the non-negative growth height floor |
| `mario.lua` `grabvine` transition branch | `iw2wth_core::player::{LegacyGrabVineContext, LegacyGrabVineOutcome, apply_legacy_grab_vine}` | Seeded | Rust tests cover the ducking reset before the portal guard, portal-suppressed no-op behavior, climb-state setup, and left/right snap plus pointing-angle outputs |
| `mario.lua` on-vine movement timer and animation gate branch | `iw2wth_core::player::{LegacyOnVineContext, LegacyOnVineDirection, LegacyOnVineUpdate, update_legacy_on_vine_motion}` | Seeded | Rust tests cover up/down timer and frame advancement, idle timer reset, direction-specific move speeds and frame delays, and the `<= vineanimationstart` trigger gate |
| `mario.lua` on-vine tile/portalwall collision clamps and down-climb `checkportalHOR` probe | `iw2wth_core::player::{LegacyOnVineContext, LegacyOnVineUpdate, update_legacy_on_vine_motion}` | Seeded | Rust tests cover up/down first-solid vertical clamps, neutral no-op collision behavior, and the down-climb portal probe Y emitted before solid clamping |
| `mario.lua`/`physics.lua` on-vine down-climb `checkportalHOR` result branch | `iw2wth_core::player::{LegacyOnVineHorizontalPortalContext, LegacyOnVineHorizontalPortalPair, LegacyOnVineHorizontalPortalOutcome, apply_legacy_on_vine_horizontal_portal}` | Seeded | Rust tests cover horizontal portal entry selection, entry speed-facing gates, successful teleport output, blocked-exit vertical bounce, vertical-opposite flag preservation, non-opposite jump/fall outputs, and exit-facing portaled callback output |
| `mario.lua` `dropvine` transition branch | `iw2wth_core::player::{LegacyDropVineContext, LegacyDropVineOutcome, apply_legacy_drop_vine}` | Seeded | Rust tests cover side-dependent `7/16` X offsets, falling animation-state swap, gravity restore, vine mask clear output, and preservation of the pre-existing `falling` boolean |
| `mario.lua` on-vine attachment-loss probe | `iw2wth_core::player::{LegacyOnVineAttachmentLossContext, apply_legacy_on_vine_attachment_loss}` | Seeded | Rust tests cover overlap-present suppression and `dropvine` delegation only after the vine overlap probe is empty |
| `mario.lua` spring and faithplate player state | `iw2wth_core::player` spring/faithplate helpers | Seeded | Rust tests cover spring latch state, held-spring X pinning, timer advance without clamp, `springytable` Y offset placement, strict `>` auto-release threshold, high-jump request gate, leave-spring forces/gravity/state, leave-spring Y formula, and faithplate animation/falling flags |
| `mario.lua` fire/upfire/hammer player hazard collision results | `iw2wth_core::player` hazard outcome helpers | Seeded | Rust tests cover floor fire vs generic enemy death causes, side/ceiling death labels, invincible ignore, and star/big-Mario ignore |
| `physics.lua` horizontal/vertical default collision response | `iw2wth_core::collision` legacy response helpers | Seeded | Rust tests cover side-specific snap positions, inward speed zeroing, target speed zeroing when present, static targets, and Lua `false` handler suppression |
| `physics.lua` `horcollision` response fixtures | `iw2wth_core` legacy collision response integration fixture test | Seeded | Rust integration test consumes `legacy_collision_responses.generated.tsv` covering left/right horizontal movement, Lua `false` handler suppression, and static targets |
| `physics.lua` `vercollision` response fixtures | `iw2wth_core` legacy collision response integration fixture test | Seeded | Rust integration test consumes `legacy_collision_responses.generated.tsv` covering up/down vertical movement, Lua `false` handler suppression, and static targets |
| `physics.lua` passive default collision response | `iw2wth_core::collision` legacy passive response helper | Seeded | Rust tests cover passive callback dispatch flags, default floor snap, downward-only speed zeroing, target velocity preservation, and Lua `false` floor handler suppression |
| `physics.lua` `passivecollision` response fixtures | `iw2wth_core` legacy passive collision response integration fixture test | Seeded | Rust integration test consumes `legacy_passive_collision_responses.generated.tsv` covering passive dispatch, default floor snap, upward-speed preservation, and Lua `false` floor handler suppression |
| Lua `map` table tile/top-gel lookups over parsed mappack levels | `iw2wth_runtime::map` adapter boundary | Seeded | Rust tests cover Lua-style 1-based tile lookup, bounds checks, and top-gel surface/payload projection |
| `main.lua` `love.update` frame-step timing gates | `iw2wth_runtime::time` adapter boundary | Seeded | Rust tests cover raw-`dt` clamp, speed-target easing/snap ordering, scaled `gdt`, `frameadvance`, and `skipupdate` behavior |
| `game.lua`/`main.lua` mappack filesystem asset lookups | `iw2wth_runtime::assets` adapter boundary | Seeded | Rust tests cover legacy settings/level path construction, custom tiles availability, custom music `.ogg` before `.mp3` precedence, numbered background stop-at-gap behavior, and SMB portal background fallback |
| `main.lua` sound-effect source setup and `game.lua` `playsound` | `iw2wth_runtime::audio` adapter boundary | Seeded | Rust tests cover legacy Lua sound globals, `.ogg` asset paths, portal sound volume overrides, score-ring looping metadata, sound-disabled suppression, and stop-before-play command sequencing |
| `main.lua` default image filtering/tile atlas loading and `game.lua` background/tile draw ordering | `iw2wth_runtime::render` adapter boundary | Seeded | Rust tests cover legacy background color tables, nearest-clamped tile atlas image specs, 17-pixel tile atlas stride counts, SMB/Portal/custom atlas thresholds, tile-batch draw ordering/scroll offsets, and custom-background parallax tiling |
| `quad.lua` tile metadata pixels plus `main.lua`/`game.lua` SMB, Portal, and custom tile atlas order | `iw2wth_runtime::tiles` adapter boundary | Seeded | Rust tests cover six alpha-flag metadata rows, PNG byte loading, SMB/Portal/custom tile ID ordering, and harness tile collision from decoded metadata |
| Lua `map[x][y][1]` plus `tilequads[...]` metadata flag lookups | `iw2wth_runtime::map::LegacyTileMetadataMapQuery` adapter boundary | Seeded | Rust tests cover Lua-style coordinate joins to decoded collision, invisible, breakable, coin-block, coin, portalability, and solid-portalable decisions |
| `game.lua`/`main.lua` one-level load/update/draw/audio adapter composition | `iw2wth_runtime::shell` minimal runtime shell | Seeded | Rust tests cover mappack settings and level loading through legacy asset paths, Lua-style map query exposure, deterministic frame-clock stepping, input projection, tile-batch render intents, background color intent, and sound restart commands |
| `mario.lua` one-player movement update plus `physics.lua` tile collision scan/resolution | `iw2wth_runtime::shell` one-player frame integration | Seeded | Rust tests cover legacy movement-before-collision ordering, gravity stepping, Lua-style map tile lookup, vertical landing snap, horizontal side snap, and adapter-owned tile solidity callback fed by Rust-decoded tile metadata |
| `game.lua`/`main.lua` local one-level runtime entry path | `iw2wth_runtime::harness` runnable deterministic shell harness | Seeded | Rust tests cover deterministic keyboard input, filesystem-style shell loading, fixed-seed one-player stepping, adapter-owned tile solidity, and explicit known parity gaps; runnable with `cargo run --manifest-path rust/Cargo.toml -p iw2wth_runtime --bin iw2wth-runtime-harness -- --repo-root . --mappack smb --level 1-1 --frames 3 --right --run` |
| `game.lua` player-spawn marker scan and `mario:new` player-1 start placement | `iw2wth_runtime::harness` adapter-side player spawn discovery | Seeded | Rust tests cover Lua row-major last-spawn-wins discovery, `startx - 6/16`/`starty - 1` player body placement, and fixed-seed fallback when no spawn marker exists |
| `mario.lua` small-player coin probe and `collectcoin` tile clear/sound/score effects | `iw2wth_runtime::shell` adapter-side player coin pickup | Seeded | Rust tests cover bottom/center small-player coin probes through `LegacyTileMetadataMapQuery`, tile clear to ID 1, coin sound commands, 200-point pickup reports, and no repeat pickup after the tile is cleared |
| `mario.lua` small-player coin pickup frame reports exposed through the local runtime harness | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` structured direct player coin-pickup details | Seeded | Rust tests cover last-frame pickup vectors, last pickup tile/clear/score/sound metadata, CLI labels, and no live counter/rendering execution |
| `game.lua` `playsound` stop/play command sequencing as emitted by `mario.lua` direct coin pickup | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` structured frame audio command details | Seeded | Rust tests cover report-only frame audio command counts, last-frame command retention, and CLI labels for direct `coinsound` stop/play ordering without live audio execution |
| `physics.lua` player tile collision scan plus `mario.lua` invisible tile floor/side handlers | `iw2wth_runtime::shell` adapter-side invisible tile collision suppression | Seeded | Rust tests cover invisible solid floor and side tiles suppressing default shell collision resolution through decoded tile metadata while preserving normal solid-tile collisions |
| `physics.lua` player ceiling collision plus `mario.lua` non-invisible tile hitblock path | `iw2wth_runtime::shell` adapter-side ceiling block-hit intent | Seeded | Rust tests cover decoded breakable/coin-block metadata on upward non-invisible ceiling hits, head-bump state, block-hit sound commands, no map mutation, and invisible-ceiling suppression |
| `mario.lua` `hitblock` block-bounce queue scheduling branch | `iw2wth_runtime::shell` adapter-side block-bounce scheduling report | Seeded | Rust tests cover ceiling hit reports scheduling Lua epsilon block bounces through the core contract, parsed contained mushroom mapping, many-coins replay suppression, small-player hitter size, and no map mutation or reward replay |
| `game.lua` blockbounce timer progression and prune branch | `iw2wth_runtime::shell` adapter-side block-bounce progression report | Seeded | Rust tests cover persistent report-only queue ownership, Lua update-before-player ordering, strict completion threshold, prune reporting, suppressed replay-spawn payloads, and no map mutation or live reward replay |
| `game.lua` blockbounce completion reward replay branch | `iw2wth_runtime::shell` adapter-side block-bounce item-spawn intent report | Seeded | Rust tests cover completed bounce item-spawn intents with source index/coordinate, big-hitter mushroom-to-flower upgrade, harness intent counting, and no map mutation or live item execution |
| `mario.lua` `hitblock` contained reward-block reveal branch | `iw2wth_runtime::shell` adapter-side contained reward-block reveal/audio intent report | Seeded | Rust tests cover report-only used-block tile-change intents, spriteset mapping, vine-versus-mushroom appearance audio intents, many-coins suppression, harness intent counting, and no map mutation or live item execution |
| runtime harness block-bounce and contained reward reveal reports previously exposed only as counts/vectors | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` structured detail summaries | Seeded | Rust tests cover last-frame block-bounce schedule, completion, item-spawn, queue prune, and contained reward reveal metadata, CLI labels, and no live item execution |
| runtime harness single/many coin-block reward and top-coin collection reports previously exposed only as counts/vectors | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` structured reward detail summaries | Seeded | Rust tests cover last-frame reward/top-coin coordinates, coin sounds, animation state, score/coin/life outcomes, tile-change metadata, many-coins timer spawns, CLI labels, and no live counter/map mutation |
| `mario.lua` `hitblock` plain coin-block reward branch | `iw2wth_runtime::shell` adapter-side single coin-block reward intent report | Seeded | Rust tests cover report-only coin sound, 200-point score, coin-block animation spawn, used-block tile-change intent, harness intent counting, and no map/counter/life mutation |
| `mario.lua` `hitblock` many-coins reward branch | `iw2wth_runtime::shell` adapter-side many-coins reward intent report | Seeded | Rust tests cover report-only coin sound, 200-point score, coin-block animation spawn, missing-timer spawn intent, expired-timer used-block tile-change intent, harness intent counting, and no map/counter/life/live-timer mutation |
| `mario.lua` `hitblock` top-coin collection branch plus `collectcoin` side effects | `iw2wth_runtime::shell` adapter-side top-coin collection intent report | Seeded | Rust tests cover report-only top-coin tile clear, coin sound, 200-point score, coin-block animation spawn, exact 100-coin life reward reporting, harness intent counting, and no map/counter/life/top-coin mutation |
| `mario.lua` reward map writes from contained reward reveal, coin-block reward, top-coin collection, and empty breakable destroy branches | `iw2wth_runtime::shell` adapter-side reward tile-change projection report | Seeded | Rust tests cover source-tagged ordered tile-change projections for contained reward reveal, coin-block reward, top-coin collection, and empty breakable destroy outcomes, plus harness projection counts and no live map mutation |
| `mario.lua` reward map writes feeding later `map` table reads | `iw2wth_runtime::shell::LegacyRuntimeProjectedTileChangeState` and projected shell metadata query | Seeded | Rust tests cover ordered reward tile-change snapshot persistence, future block-hit/top-coin query suppression from projected used/cleared tiles, and unchanged parsed level tiles |
| `mario.lua` reward map write history exposed through the local runtime harness | `iw2wth_runtime::harness` projected reward tile-change snapshot summaries | Seeded | Rust tests cover final/frame snapshot counts, ordered projection history, and no live map mutation |
| `mario.lua` `collectcoin`, single/many coin-block reward, and top-coin counter branches | `iw2wth_runtime::shell` adapter-side coin-count/life-reward counter intent report | Seeded | Rust tests cover small-player coin pickups, single coin-block rewards, many-coins rewards, top-coin collections, exact 100-coin reset/life reward behavior from shell snapshots, disabled-life reward flags, harness counter/life counts, and no live counter mutation |
| `game.lua` `addpoints` calls from player coin pickups, coin-block rewards, enemy-on-top shots, and empty block breaks | `iw2wth_runtime::shell` adapter-side score-counter and immediate scrolling-score intent report | Seeded | Rust tests cover score snapshot chaining without live mutation, no immediate scrolling score for no-coordinate `addpoints`, enemy-shot scrolling score coordinates with x-scroll capture, empty-break score reports, and harness score/scrolling-score counts |
| `coinblockanimation.lua` frame-31 `addpoints(-200, x, y)` branch | `iw2wth_runtime::shell` adapter-side coin-block animation progress report | Seeded | Rust tests cover report-only animation queue progression, deferred 200-point scrolling-score intent with x-scroll capture, queue pruning, and no live score mutation |
| `scrollingscore.lua` update/draw presentation branch for scores spawned by `game.lua` `addpoints` | `iw2wth_runtime::shell` adapter-side scrolling-score animation progress report | Seeded | Rust tests cover report-only timer progression, strict removal threshold/pruning, numeric-score presentation offset, harness progress/prune count exposure, and no live rendering/score mutation |
| local runtime harness report/output | `iw2wth_runtime::harness` report-only effect animation progress/prune counters | Seeded | Rust tests cover nonzero coin-block, block-debris, and scrolling-score animation progress/prune counts from local harness scenarios, including final-frame summaries |
| runtime harness effect animation progress/prune reports previously exposed only as counts/vectors | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` structured detail summaries | Seeded | Rust tests cover last-frame and last-prune coin-block animation, block-debris animation, and scrolling-score animation metadata, CLI labels, and no live rendering |
| `quad.lua` portalability metadata joined to the current parsed level | `iw2wth_runtime::harness` report-only current-level portalability summary | Seeded | Rust tests cover queried, portalable, solid-portalable, and solid-non-portalable counts from adapter-side tile metadata without live portal placement or physics |
| `mario.lua` portal aim/input checks plus `game.lua`/`physics.lua` portalable tile target probing | `iw2wth_runtime::shell` adapter-side report-only portal target probe | Seeded | Rust tests cover active/inactive aim snapshots, requested slot preservation, trace hit coordinates/sides/tendency, solid-portalable placement checks, blocked-surface rejection, harness counts, and no live portal reservations |
| `game.lua` `shootportal`/`createportal` requested slot open outcome plus rejected target fizzle reporting | `iw2wth_runtime::shell` adapter-side portal open/fizzle outcome intent report | Seeded | Rust tests cover slot-specific open/fizzle outcome reports, portal open/fizzle sound intents, harness/CLI counts, and no live portal placement or physics |
| `game.lua` `shootportal`/`createportal` target placement and open/fizzle summaries exposed through the local runtime harness | `iw2wth_runtime::harness` structured portal target placement and open outcome summaries | Seeded | Rust tests cover possible/impossible placement summaries, last trace/placement details, open/fizzle outcome summaries, sound intents, harness fields printed by the CLI, and no live portal/player mutation |
| `game.lua` `createportal` tile removal and `portalwall:new` side-specific reservation setup | `iw2wth_runtime::shell` adapter-side portal tile/wall reservation projection report | Seeded | Rust tests cover Lua `modifyportaltiles` two-tile spans, side-specific `portalwall:new` rectangles, harness/CLI counts, and no live portal placement or physics |
| `game.lua` `createportal` portal slot fields after tile/wall reservation setup | `iw2wth_runtime::shell` adapter-side projected portal state snapshot | Seeded | Rust tests cover per-slot projection persistence, same-slot replacement, derived future block-guard reservations, harness exposure, and no live portal physics |
| `game.lua` projected portal slot state exposed through the local runtime harness | `iw2wth_runtime::harness` projected portal state snapshot summaries | Seeded | Rust tests cover final/frame projected-slot snapshot counts, preserved per-slot projection state, CLI count output, and no live portal physics |
| `mario.lua` `hitblock` portal guard with adapter-projected `game.lua` portal slots | `iw2wth_runtime::shell` adapter-side projected portal block-hit guard context | Seeded | Rust tests cover explicit reservation preservation plus projected portal-state suppression of block-hit sound, bounce, and breakable-block side effects without live portal physics |
| `mario.lua` `hitblock` portal guard details exposed through the local runtime harness | `iw2wth_runtime::harness` structured projected portal block-hit guard summaries | Seeded | Rust tests cover projected-versus-explicit guard counts, last protected tile, guarded tile ID/breakable/coin-block metadata, hit-sound suppression, reservation facing/source metadata, CLI-facing summary fields, and no live portal physics/block effects |
| `game.lua` `createportal` same-slot replacement branch | `iw2wth_runtime::shell` and `iw2wth_runtime::harness` adapter-side portal replacement summaries | Seeded | Rust tests cover previous/replacement slot summaries, preserved peer-slot state, same-placement no-op suppression, harness count exposure, and no live portal physics |
| `game.lua` `createportal` reservation and replacement reports exposed through the local runtime harness | `iw2wth_runtime::harness` structured portal reservation/replacement summaries | Seeded | Rust tests cover last-frame reservation tile/wall detail, previous/replacement/preserved slot detail, CLI-facing summary fields, and no live portal physics |
| `game.lua`/`physics.lua` portal pairing readiness gate (`portal1X ~= false and portal2X ~= false`) | `iw2wth_runtime::shell` and `iw2wth_runtime::harness` adapter-side portal-pair readiness summaries | Seeded | Rust tests cover empty/no-summary state, one-slot not-ready summary, both-slot entry/exit pairing metadata, harness count exposure, and no live portal physics/transit |
| `physics.lua` `inportal` player center tile probe over ready portal pairs | `iw2wth_runtime::shell` and `iw2wth_runtime::harness` adapter-side portal-transit candidate probe | Seeded | Rust tests cover Lua-style player body center tile math, ready-pair entry/exit slot metadata, non-matching center tiles, harness count exposure, and no live portal physics/player mutation |
| `physics.lua` `inportal` call into `portalcoords` for an unblocked portal exit | `iw2wth_runtime::shell` and `iw2wth_runtime::harness` adapter-side portalcoords preview report | Seeded | Rust tests cover report-only entry/exit facing, player body/speed/rotation/animation-direction output metadata, harness count exposure, and no live teleport/player mutation |
| `physics.lua` `checkportalHOR` blocked-exit `checkrect` bounce branch | `iw2wth_runtime::shell` adapter-side portalcoords blocked-exit probe | Seeded | Rust tests cover projected tile metadata blocking at the preview output body, Lua vertical blocked-exit bounce min-speed metadata, and no live teleport/player mutation |
| `physics.lua` `inportal` portalcoords success versus blocked-exit branch | `iw2wth_runtime::shell` and `iw2wth_runtime::harness` adapter-side portal-transit outcome summary | Seeded | Rust tests cover successful teleport preview versus blocked-exit bounce preview classification, output body/speed metadata, harness/CLI counts, and no live portal physics/player mutation |
| `game.lua`/`physics.lua` portal readiness, candidate, and transit outcome reports exposed through the local runtime harness | `iw2wth_runtime::harness` structured portal readiness/candidate/outcome summaries | Seeded | Rust tests cover last-frame readiness, center-tile candidate, and outcome detail retention for CLI reporting with no live portal physics/player mutation |
| `physics.lua` portal transit player mutation outputs | `iw2wth_runtime::shell` and `iw2wth_runtime::harness` adapter-side projected player-state snapshot | Seeded | Rust tests cover teleport output body/speed/animation-direction snapshots, blocked-exit bounce speed with the original body, harness/CLI counts, and no live player mutation |
| `physics.lua` player state after portal transit feeding the next `inportal` probe | `iw2wth_runtime::shell` adapter-side projected player-state source for portal-transit probes | Seeded | Rust tests cover a second frame using the projected body/speed for portal-transit candidate probing while the live player body remains unchanged |
| `physics.lua` player state after portal transit feeding later `shootportal` source coordinates | `iw2wth_runtime::shell` adapter-side projected player-state source for portal target probes | Seeded | Rust tests cover a later active portal aim trace using the projected body source while the live player body remains unchanged |
| `physics.lua` player state after portal transit feeding later `shootportal` source coordinates through local harness reporting | `iw2wth_runtime::harness` projected-player-source portal target probe exposure | Seeded | Rust tests cover seeded projected player state, structured live/projected source-selection summaries, projected ray source coordinates, CLI source-selection output, and unchanged live player body |
| `physics.lua` player portal pass sound branch | `iw2wth_runtime::shell` and `iw2wth_runtime::harness` adapter-side portal-enter audio intent report | Seeded | Rust tests cover portal-enter sound intents for successful and blocked-exit transit outcomes, harness/CLI counts, and no live audio execution or player mutation |
| `physics.lua` portalcoords, player portal pass sound, and portal transit player mutation reports exposed through the local runtime harness | `iw2wth_runtime::harness` structured portalcoords/audio/projected-player summaries | Seeded | Rust tests cover frame-indexed last-detail summaries for portalcoords previews, portal-enter audio intents, projected player-state snapshots, CLI-facing report fields, and no live portal physics/audio/player mutation |
| `game.lua` `coinblocktimers` positive-only countdown branch in the runtime frame loop | `iw2wth_runtime::shell` adapter-side many-coins timer progress report | Seeded | Rust tests cover report-only timer countdown from shell snapshots, zero/negative no-op behavior, preserved negative overshoot for same-frame many-coins block-hit lookup, harness progress counting, and no live timer mutation |
| `mario.lua` missing `coinblocktimers` insertion after many-coins rewards | `iw2wth_runtime::shell` adapter-side many-coins timer start/projection report | Seeded | Rust tests cover reward-indexed timer start reports, after-countdown projected timer tables, harness start counting, and no live timer mutation |
| runtime harness jump-item, top-enemy, and many-coins timer snapshots previously only available to Rust tests | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` deterministic CLI seed flags | Seeded | Rust tests cover CLI parsing for repeatable jump-item, top-enemy, and many-coins timer seed flags without mutating live Lua-owned object/timer systems |
| runtime harness item jump, enemy shot, and many-coins timer reports previously exposed only as counts/vectors | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` structured detail summaries | Seeded | Rust tests cover last-frame item jump block/item/source metadata, enemy shot block/direction/score metadata, many-coins timer progress/start metadata, and no live item/enemy/timer mutation |
| `game.lua` `jumpitems` and `mario.lua` `hitblock` item-on-top jump loop | `iw2wth_runtime::shell` adapter-side item-on-top jump request intent report | Seeded | Rust tests cover adapter-provided mushroom/one-up snapshots, inclusive center-range checks, exact tile-top bottom alignment, missing jump-handler suppression, portal-guard suppression, source-block X propagation, harness intent counting, and no live item mutation |
| `game.lua` `enemies` and `mario.lua` `hitblock` enemy-on-top shot loop | `iw2wth_runtime::shell` adapter-side enemy-on-top shot request intent report | Seeded | Rust tests cover adapter-provided enemy snapshots, inclusive center-range checks, exact tile-top bottom alignment, left/right shot direction from the block center, 100-point score placement, missing shotted-handler suppression, duplicate probe preservation, portal-guard suppression, harness intent counting, and no live enemy mutation |
| `mario.lua` `hitblock` empty breakable-block destroy branch plus `destroyblock` side effects | `iw2wth_runtime::shell` adapter-side empty breakable-block destroy intent report | Seeded | Rust tests cover big-player eligibility, portal-protected no-op reporting, portal-guarded suppression of block-hit/block-break sounds and bounce/reward side effects, clear-tile/clear-gel/tile-object-removal intents, block-break sound, 50-point score, debris intents, sprite-batch regeneration, harness intent counting, and no map/gels/collision-object/live-debris mutation |
| `mario.lua` `destroyblock` cleanup side-effect ordering | `iw2wth_runtime::shell` adapter-side breakable-block cleanup projection report | Seeded | Rust tests cover report-only tile-collision object removal, gel clearing, four ordered debris spawn projections, sprite-batch regeneration, harness projection counting, and no live object/gels/debris/sprite-batch mutation |
| runtime harness reward tile-change and breakable-block cleanup projection reports previously exposed only as counts/vectors | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` structured projection detail summaries | Seeded | Rust tests cover ordered reward tile-change source/tile metadata, projected snapshot retention, ordered cleanup action/debris/gels metadata, CLI labels, and no live map/object/gels/debris mutation |
| `blockdebris.lua` update branch for debris spawned by `mario.lua` `destroyblock` | `iw2wth_runtime::shell` adapter-side block-debris animation progress report | Seeded | Rust tests cover following-frame report-only debris progression, gravity-before-motion state updates, strict animation frame toggles, removal pruning, harness progress counts, and no live rendering/debris mutation |
| runtime harness projected portal/player seeds previously only available to Rust tests | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` deterministic CLI seed flags | Seeded | Rust tests cover CLI parsing for projected portal slot seeds and forced player body/speed seeds, plus configured player seeds overriding legacy spawn discovery only when explicitly requested |
| `variables.lua` `spacerunroom` and `mario.lua` side tile gap-run response | `iw2wth_core::config` and `iw2wth_core::player` side tile gap-run helper | Seeded | Rust tests cover right/left side snap offsets, top snap, vertical speed clear, running animation state, open-above-tile gate, falling-speed gate, and strict room threshold |
| `mario.lua` invisible tile player collision suppression | `iw2wth_core::player` invisible tile suppression helpers | Seeded | Rust tests cover floor pre-collision state restore, side default suppression, ceiling position/speed gate, and non-invisible no-op behavior |
| `mario.lua` side button collision response | `iw2wth_core::player` side button response helper | Seeded | Rust tests cover right/left side snap offsets, top snap, downward-only speed clear, and preserved jumping/upward state |
| `mario.lua` side box collision response | `iw2wth_core::player` side box response helper | Seeded | Rust tests cover right/left speed damping, strict half-walk threshold, unblocked box speed transfer/default suppression, and blocked-box default collision preservation |
| `mario.lua` right-side pipe entry decision | `iw2wth_core::player` side pipe entry helper | Seeded | Rust tests cover grounded right/intermission gate, current-tile pipe priority, below-tile pipe fallback, sublevel propagation, and airborne rejection |
| `mario.lua` non-invisible ceiling tile response | `iw2wth_core::player` ceiling tile response helper | Seeded | Rust tests cover big-Mario block break, left/right neighbor hit retargeting, clear-space side shove, blocked-shove hitblock fallback, head-bump state, and side-speed zeroing |
| `game.lua` `checkkey`, `runkey`, `rightkey`, and `leftkey` input polling | `iw2wth_runtime::input` adapter boundary | Seeded | Rust tests cover keyboard polling, joystick hat/button/axis polling, strict deadzone thresholds, missing joystick/button false behavior, and independent movement key projection into `PlayerMovementInput` |
| runtime harness coin/life/score/scrolling-score reports previously exposed only as counts/vectors | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` structured detail summaries | Seeded | Rust tests cover last-frame coin source/count/score metadata, life-reward payload metadata, score source before/delta/after metadata, scrolling-score label/coordinate metadata, and CLI source labels without mutating live counters |
| runtime harness player tile-collision reports previously exposed only as counts/frame vectors | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` structured tile-collision detail summaries | Seeded | Rust tests cover last-frame horizontal collision, vertical collision, and ceiling block-hit metadata plus CLI labels without mutating live rendering or gameplay systems |
| `mario.lua` block-hit and contained reward-reveal `playsound` sequencing exposed through aggregate frame audio commands | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` source-specific frame audio command details | Seeded | Rust tests cover block-hit and contained reward-reveal stop/play command detail retention for CLI reporting without live audio execution |
| `mario.lua` coin-block/top-coin coin sounds, `destroyblock` block-break sound, and `game.lua` portal open/fizzle sounds exposed through aggregate frame audio commands | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` source-specific frame audio command details | Seeded | Rust tests cover coin-block reward, top-coin collection, block-break, and portal fizzle stop/play command detail retention for CLI reporting without live audio execution |
| `physics.lua` portal pass `playsound(portalentersound)` sequencing exposed through aggregate frame audio commands | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` source-specific portal-transit frame audio command details | Seeded | Rust tests cover portal-enter stop/play command detail retention for successful and blocked-exit transit reports without live audio execution |
| `mario.lua` `fire` launch branch plus `fireball.lua` spawn setup | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` report-only player fireball launch intent | Seeded | Rust tests cover flower/ducking/max-count fire guards, source/spawn offsets, direction speed, fireball-count increment, fire-animation timer reset, fireball sound commands, CLI flags, and no live projectile execution |
| `fireball.lua` `update` animation/offscreen removal branch | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` report-only player fireball projectile progression | Seeded | Rust tests cover launch-to-progress handoff, strict fireball animation timer advancement, offscreen release/removal pruning, deterministic seeded projectile CLI input, harness/CLI detail summaries, and no live projectile collision execution |
| `fireball.lua` offscreen update removal plus `mario.lua` `fireballcallback` release branch | `iw2wth_runtime::shell` adapter-side fireball projectile update release summaries | Seeded | Rust tests cover offscreen `released_thrower` update summaries, `mario:fireballcallback` count-delta metadata, projected active-fireball count snapshots, and no live player-counter mutation |
| runtime harness fireball update release summaries previously exposed only through progress flags | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` structured update-release detail summaries | Seeded | Rust tests cover last-frame offscreen release summary metadata, projectile-update source labels, projected count detail labels, and no live player-counter mutation |
| `fireball.lua` side/floor/ceiling/passive collision branches | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` report-only player fireball collision probe outcomes | Seeded | Rust tests cover cloned projectile-state probes for side/floor/ceiling/passive collision contracts, deterministic CLI probe input, structured harness detail summaries, and no live projectile/enemy/block/audio/counter mutation |
| `fireball.lua` side/floor/ceiling/passive collision branches | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` report-only player fireball collision outcome probe | Seeded | Rust tests cover cloned projectile probe state for side, floor, ceiling, and passive collisions, deterministic CLI probe input, harness detail summaries, block-hit sound/enemy score metadata, and no live enemy/block/projectile mutation |
| `fireball.lua` `hitstuff` `playsound(blockhitsound)` and `addpoints(firepoints[a], self.x, self.y)` branches | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` fireball collision frame audio and score summaries | Seeded | Rust tests cover report-only block-hit sound commands, fireball-collision score source labels, scrolling-score coordinates from cloned projectile outcomes, and no live audio/counter/enemy/block/projectile mutation |
| `mario.lua` `fireballcount` and `mario:fireballcallback` count deltas from report-only fireball launch/collision/update release paths | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` projected active-fireball count snapshots | Seeded | Rust tests cover launch, collision-release, and update-removal release before/delta/after snapshots, projected count use in later fire guards, structured harness/CLI labels, and no live player-counter mutation |
| `fireball.lua` `explode` and `mario.lua` `fireballcallback` release branch | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` fireball collision release summaries | Seeded | Rust tests cover collision-derived `mario:fireballcallback` count-delta metadata, structured harness/CLI release detail summaries, and no live projectile queue or fireball counter mutation |
| `physics.lua` fireball tile scan over `tilequads` metadata | `iw2wth_runtime::shell` adapter-side fireball map/tile target probe | Seeded | Rust tests cover projectile-position target probes through Rust-decoded tile metadata, non-player invisible-tile skipping, target coordinate/tile/axis/block-hit-sound metadata, and no live projectile collision mutation |
| projected reward map writes feeding later `physics.lua` fireball tile scans | `iw2wth_runtime::shell` projected tile-change reads for fireball map/tile target probes | Seeded | Rust tests cover projected tile changes steering fireball target probes while leaving the parsed level unchanged and avoiding live collision mutation |
| runtime harness fireball map/tile target probes previously unavailable outside shell tests | `iw2wth_runtime::harness` and `iw2wth-runtime-harness` structured fireball map-target detail summaries | Seeded | Rust tests cover final/frame probe counts, last target tile flags, CLI labels, block-hit-sound metadata, and no live collision mutation |
| `physics.lua` fireball tile scan handoff into `fireball.lua` tile collision branches | `iw2wth_runtime::shell` map-derived report-only fireball tile collision outcome and release summaries | Seeded | Rust tests cover side-tile block-hit/release/projected-count summaries, floor-tile bounce without release/audio, harness detail exposure, and no live projectile collision mutation |
| `fireball.lua` explicit collision probes versus `physics.lua` map-derived tile collision handoff | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` fireball collision source provenance | Seeded | Rust tests cover explicit-request versus map-target source labels, separate harness source counts, release/projected-count provenance, and no live projectile/counter mutation |
| `fireball.lua` `hitstuff` enemy `addpoints` branch plus explicit collision probe source | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` fireball collision score-counter source provenance | Seeded | Rust tests cover explicit-request source preservation in score-counter summaries and CLI score source labels without live score/enemy/block/audio/projectile mutation |
| `fireball.lua` `hitstuff` enemy `addpoints` branch feeding `scrollingscore.lua` animations | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` fireball collision scrolling-score animation source provenance | Seeded | Rust tests cover explicit-request source preservation in queued and progressed scrolling-score animation summaries, source-specific harness frame/final counts, CLI labels, and no live rendering/enemy mutation |
| `fireball.lua` `hitstuff` enemy `b:shotted("right")` branch from explicit collision probes | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` adapter-side fireball enemy-hit intent summaries | Seeded | Rust tests cover adapter-provided enemy snapshots, explicit-request-only hit summaries, rightward shot direction, optional score metadata, CLI seed/detail labels, and no live enemy mutation |
| `fireball.lua` enemy `shotted` removal effects on later fireball score probes | `iw2wth_runtime::shell` projected fireball enemy hit/removal score gate | Seeded | Rust tests cover first-hit score preservation, later same-enemy score/scrolling-score suppression through projected removal state, and no live enemy mutation |
| `fireball.lua` enemy `b:shotted("right")` post-hit active/shot state | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` adapter-side projected fireball enemy hit/removal snapshots | Seeded | Rust tests cover projected inactive/shot state, suppression of later report-only enemy-hit queries for the same snapshot, harness/CLI detail labels, and no live enemy mutation |
| `physics.lua` passive fireball/enemy overlap dispatch plus `fireball.lua` `hitstuff` enemy branch | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` adapter-side report-only fireball enemy-overlap probes | Seeded | Rust tests cover automatic overlap probe source labels from adapter enemy snapshots, projected removal suppression, `b:shotted("right")` metadata, firepoints score provenance, harness/CLI detail counts, and no live projectile/enemy mutation |
| `physics.lua` fireball/enemy overlap ordering plus `fireball.lua` `hitstuff` representative target outcomes | `iw2wth_runtime::shell` Lua-generated fireball enemy overlap fixture test | Seeded | Runtime shell test consumes `legacy_fireball_enemy_overlaps.generated.tsv` covering passive/horizontal/vertical overlap selection, missing-handler skip, Bowser no-score, beetle no-shot/no-score, and no live projectile/enemy mutation |
| `fireball.lua` collision-mutated projectile state after `hitstuff` from report-only collision probes | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` adapter-side projected fireball projectile collision snapshots | Seeded | Rust tests cover first-probe-per-projectile projection, later automatic overlap suppression against a second enemy snapshot, structured harness/CLI detail labels, and no live projectile queue mutation |
| `fireball.lua` explosion animation/removal after collision-mutated projectile state | `iw2wth_runtime::shell` and `iw2wth_runtime::harness` report-only projected fireball projectile progression previews | Seeded | Rust tests cover projected collision state feeding later projectile progress reports, strict explosion frame timing through the runtime frame clock, projected removal/prune metadata, harness detail exposure, and no live projectile queue mutation |
| `fireball.lua` draw branch for flying and explosion projectile frames | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` report-only fireball projectile render-intent previews | Seeded | Rust tests cover live/projected projectile render preview sources, flying versus explosion frame/quad metadata, projected removal suppression, structured harness/CLI detail labels, and no live rendering or projectile queue mutation |
| `mario.lua` player graphic/quad selection and `game.lua` player draw call metadata | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` report-only player render-intent previews | Seeded | Rust tests cover one-player body/facing/render-frame/size/power-up/ducking/fire-animation metadata, frame-selection matrix coverage, structured harness/CLI detail labels, and no live rendering or player mutation |
| `main.lua` player animation quad tables and `mario.lua` `setquad` atlas selection | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` player render-intent quad/source-rectangle previews | Seeded | Rust tests cover small/big run, idle, slide, jump, swim, climb, dead, duck, and fire atlas source rectangles, harness detail retention, CLI quad labels, and no live rendering or player mutation |
| `main.lua` player animation layer image loading, `mario.lua` player color table selection, and `game.lua` `graphic[1..3]`/`graphic[0]` draw order | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` player render-intent color-layer image previews | Seeded | Rust tests cover small/big animation layer image paths, legacy `graphic[1]`, `graphic[2]`, `graphic[3]`, then `graphic[0]` ordering, player/flower/white tint metadata, CLI color-layer labels, and no live rendering or player mutation |
| `hatconfigs.lua`, `bighatconfigs.lua`, and `game.lua` player hat draw branch | `iw2wth_runtime::shell`, `iw2wth_runtime::harness`, and `iw2wth-runtime-harness` player render-intent hat draw previews | Seeded | Rust tests cover default hat slots, small/big hat image/config offsets, standard-hat tinting, stacked origins, post-`graphic[1..3]` and pre-`graphic[0]` ordering metadata, CLI hat labels, and no live rendering or player mutation |
